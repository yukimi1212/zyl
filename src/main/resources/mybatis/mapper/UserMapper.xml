<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zucc.smart.mapper.UserMapper">

	<resultMap id="UserMap" type="com.zucc.smart.domain.User">
		<result column="user_id" property="user_id" />
		<result column="user_name" property="user_name" />
		<result column="user_pwd" property="user_pwd" />
		<result column="user_gender" property="user_gender" />
		<result column="user_age" property="user_age" />
		<result column="user_phone" property="user_phone" />
		<result column="registration_time" property="registration_time" />	
	</resultMap>

	<select id="checkUser" resultMap="UserMap" useCache="false">
    	select user_id, user_name, user_pwd, user_gender, user_age, user_phone, registration_time 
    	FROM user  
    	where user_id = #{user_id} and user_pwd = #{user_pwd}
    </select>
    
	<select id="getUserById" parameterType="java.lang.String" resultMap="UserMap" useCache="false">
		SELECT user_id, user_name, user_pwd, user_gender, user_age, user_phone, registration_time
		FROM user
		where user_id = #{user_id}
	</select>
	
	<insert id="saveUser" parameterType="com.zucc.smart.domain.User">
		INSERT INTO user
		(user_id, user_name, user_pwd, user_gender, user_age, user_phone, registration_time)
		values
		(#{user_id}, #{user_name}, #{user_pwd}, #{user_gender}, #{user_age}, #{user_phone}, now())
	</insert>
    
    <delete id="deleteUser" parameterType="java.lang.String">
		DELETE
		FROM user
		where user_id = #{user_id}
	</delete>
	
<!--<insert id="saveUser" parameterType="com.zucc.smart.domain.User">
    	insert into user (user_id, user_name, user_pwd, 
    	<trim suffixOverrides=",">
			<if test="user_gender != null and user_gender != ''">
		      user_gender,
		    </if>
			<if test="user_age != null and user_age != ''">
		      user_age,
		    </if>
		    <if test=" user_phone != null and user_phone != ''">
		      user_phone,
		    </if>
		</trim>   
		)VALUES(
		#{user_id}, #{user_name}, #{user_pwd},
		<trim suffixOverrides=",">
			<if test="user_gender != null and user_gender != ''">
		  		#{user_gender},
			</if>
			<if test="user_age != null and user_age != ''">
		  		#{user_age},
			</if>
			<if test="user_phone != null and user_phone != ''">
		  		#{user_phone},
			</if>
		</trim>
		)
    </insert>
 -->        
   
</mapper>